# k8s-container-resource-optimizer
A bash script that shows which container is using more resources than it really needs.

## Using
Showing Container for all namespaces
```
./optimize.sh
```

Showing Container for specific namespace
```
./optimize.sh default
```